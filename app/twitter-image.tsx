// Reuse the Open Graph card for the Twitter/X image so twitter:image is
// always populated (twitter card is summary_large_image on the homepage).
export { default, alt, size, contentType } from "./opengraph-image";
